### git secret toruble
if u secret trouble to push git with Sample_Dev_Codes of Books
- use .gitignore, thats uncompressed secret issued codes 

if u in secret trouble mode? git history has a secret commit, clear .git and re-push
```
$ rm -Rf .git
$ git init
$ gitjoe (git remote add origin)
$ git push -u -f
```
+==
